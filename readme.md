HTML Registration Form

This project demostrates core web form structure using plain HTML.

Features:

Built in HTML elements (form, label, input, button)

Usage:

Simply clone the repo and open the file in any web browser to view and test the form.